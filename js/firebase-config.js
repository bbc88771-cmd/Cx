// Конфигурация Firebase-проекта.
// Замените значения ниже на конфиг вашего собственного Firebase-проекта
// (Firebase Console → Project settings → Your apps → SDK setup and configuration).
// Эти значения НЕ секретны — Firebase Web SDK config можно спокойно коммитить в публичный
// репозиторий, безопасность обеспечивается правилами Firestore (см. firestore.rules).
export const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "YOUR_SENDER_ID",
  appId: "YOUR_APP_ID",
};
